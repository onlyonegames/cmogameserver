package com.onlyonegames.eternalfantasia.domain.repository;

import com.onlyonegames.eternalfantasia.domain.model.entity.BigIntegerTest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BigIntegerTestRepository extends JpaRepository<BigIntegerTest, Long> {
}
